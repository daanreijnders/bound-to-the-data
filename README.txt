# Bound to the Data

Simple Python module/script with set of functions as classes to download oceanographic environemental forecast data (atmospheric, waves, currents, biogeochemistry). Includes routines to access data at a particular (lon, lat) coordinate pair and export this to JSON.

Documentation to follow.
